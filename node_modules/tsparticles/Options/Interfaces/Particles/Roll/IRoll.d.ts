import type { RangeValue, SingleOrMultiple } from "tsparticles/Types";
import type { IColor } from "tsparticles/Core/Interfaces";
import type { IRollLight } from "tsparticles/Options/Interfaces/Particles/Roll/IRollLight";
import type { RollMode } from "tsparticles/Enums";
export interface IRoll {
    backColor?: SingleOrMultiple<string> | IColor;
    darken: IRollLight;
    enable: boolean;
    enlighten: IRollLight;
    mode: RollMode | keyof typeof RollMode;
    speed: RangeValue;
}

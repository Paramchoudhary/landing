import { Range } from "tsparticles/Utils/Range";
import type { ICoordinates } from "tsparticles/Core/Interfaces/ICoordinates";
import { Circle } from "tsparticles/Utils/Circle";
import type { IDimension } from "tsparticles/Core/Interfaces/IDimension";
export declare class CircleWarp extends Circle {
    private readonly canvasSize;
    constructor(x: number, y: number, radius: number, canvasSize: IDimension);
    contains(point: ICoordinates): boolean;
    intersects(range: Range): boolean;
}

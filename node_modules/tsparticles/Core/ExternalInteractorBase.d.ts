import type { IDelta, IExternalInteractor } from "tsparticles/Core/Interfaces";
import type { Particle } from "tsparticles/Core/Particle";
import type { Container } from "tsparticles/Core/Container";
import { InteractorType } from "tsparticles/Enums";
export declare abstract class ExternalInteractorBase implements IExternalInteractor {
    protected readonly container: Container;
    protected constructor(container: Container);
    type: InteractorType;
    abstract interact(delta: IDelta): void;
    abstract isEnabled(): boolean;
    abstract reset(particle: Particle): void;
}

import type { Particle } from "tsparticles/Core/Particle";
import type { IInteractor } from "tsparticles/Core/Interfaces/IInteractor";
import type { IDelta } from "tsparticles/Core/Interfaces/IDelta";
export interface IParticlesInteractor extends IInteractor {
    isEnabled(particle: Particle): boolean;
    interact(particle: Particle, delta: IDelta): void;
}
